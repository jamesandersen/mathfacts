export enum Mode {
        STARTUP = 0,
        PRACTICE = 1,
        TIMED = 2
};

export enum Operation {
        ADDITION = 0x1,
        SUBTRACTION  = 0x2,
        MULTIPLICATION = 0x4,
        DIVISION = 0x8
}